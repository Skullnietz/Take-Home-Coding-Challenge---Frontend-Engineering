export type Profile = {
  id: string
  firstName: string
  lastName: string
  email: string
  role: string
  avatarUrl?: string
  phone?: string
  bio?: string
  createdAt: string
  updatedAt: string
}
